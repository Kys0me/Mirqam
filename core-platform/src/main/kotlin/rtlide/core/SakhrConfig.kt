package rtlide.core

/**
 * Global configuration for the Sakhr language and compiler.
 */
object SakhrConfig {
    /** The root directory where the Sakhr compiler and libraries are installed. */
    const val COMPILER_ROOT = "/home/kys0adam/.local/lib/صخر/"

    /** The name of the compiler executable (available in system PATH). */
    const val COMPILER_NAME = "صخر"

    /** The absolute path to the standard library "الأم". */
    const val STDLIB_PATH = "/home/kys0adam/.local/lib/صخر/الأم"
}
